<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/styles/style.css" />

    <title>صبا نوریانی، کاردرمانگر</title>
  </head>
  <body>
    <!-- Header -->
    <header>
      <div class="header">
        <h1>صبا نوریانی</h1>
        <nav>
          <a href="/">خانه</a>
          <a href="/#about">درباره من</a>
          <a href="/#schedule">برنامه حضور</a>
          <a href="/#services">خدمات</a>
          <a href="/blog">وبلاگ</a>
          <a href="tel: +989123334455" class="book-nav">☎️رزرو نوبت</a>
        </nav>
      </div>
    </header>

    <section
      class="container"
      style="
        display: flex;
        flex-direction: column;
        align-items: center;
        padding-bottom: 2vh;
      "
    >
      <h2>افزودن پست جدید</h2>
      <form
        action="/super-admin-dashboard"
        method="POST"
        class="form"
        enctype="multipart/form-data"
      >
        <input type="text" name="title" placeholder="عنوان پست" required />
        <!-- <input
          type="text"
          name="banner"
          placeholder="آدرس تصویر (banner)"
          required
        /> -->
        <label style="font-size: 1.2rem">آپلود عکس:</label>
        <input type="file" name="image" accept="image/*" required />
        <textarea
          name="summary"
          placeholder="خلاصه مطلب"
          maxlength="200"
          style="height: 200px"
          required
        ></textarea>
        <textarea name="content" placeholder="متن کامل" required></textarea>
        <button type="submit">ثبت مطلب</button>
      </form>
    </section>
    <hr />
    <section
      class="container"
      style="display: flex; flex-direction: column; align-items: center"
    >
      <h2>حذف پست‌ها</h2>
      <% posts.forEach(post => { %>
      <div class="post-preview" style="padding: 10px; width: 60vw">
        <h3><%= post.title %></h3>
        <p><%- post.summary %></p>
        <form
          action="/delete-post/<%= post.id %>"
          method="POST"
          onsubmit="return confirm('Are you sure you want to delete this post?');"
          class="form"
        >
          <button type="submit">حذف</button>
        </form>
      </div>
      <% }) %>
    </section>
    <div class="center-div">
      <a href="/logout" class="btn">خروج از حساب کاربری</a>
    </div>

    <footer>صبا نوریانی 1404. تمامی حقوق محفوظ است</footer>
    <script
      src="https://cdn.tiny.cloud/1/no-api-key/tinymce/6/tinymce.min.js"
      referrerpolicy="origin"
    ></script>
    <script>
      tinymce.init({
        selector: "#editor",
        directionality: "rtl",
        plugins: "lists link image code table",
        toolbar:
          "undo redo | blocks | bold italic underline | alignleft aligncenter alignright | bullist numlist | image | code",
        height: 400,
        content_style:
          "body { font-family:Vazirmatn,Arial,sans-serif; font-size:16px }",
      });
    </script>
  </body>
</html>
